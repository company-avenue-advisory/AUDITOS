fastapi[standard]
python-multipart
pymupdf
docling==2.127.0
pdfplumber>=0.11.10
pandas
openpyxl
pydantic
python-dotenv
openai
sqlalchemy
tenacity
google-cloud-storage
psycopg2-binary
celery
redis
pyjwt
bcrypt

# Observability
sentry-sdk[fastapi]>=2.69.2

# Image processing & OCR (Document Utilities)
numpy>=2.5.3
opencv-python-headless>=5.0.0.93
pillow>=12.3.0

# OCR
# easyocr is imported unconditionally by services/document_core.py, so it's
# a hard dependency, not optional -- previously commented out here, but the
# import was never actually skippable (~250MB download, unavoidable).
easyocr>=1.7.2
# pytesseract>=0.3.10      # Tier 2 OCR (requires system tesseract-ocr binary)

# Google Drive integration
google-auth>=2.58.0
google-auth-oauthlib>=1.4.1
google-auth-httplib2>=0.4.2
google-api-python-client>=2.200.0
